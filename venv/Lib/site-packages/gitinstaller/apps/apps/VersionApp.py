from ..BaseApp import BaseApp
from Config import VERSION_F
from project.VersionManager import VersionManager

class App(BaseApp):
	"Show version"
	
	def main(self):
		print(VersionManager(VERSION_F).getVersion())
	
	# === PROTECTED ===
	
	def options(self):
		return ["--version"]
