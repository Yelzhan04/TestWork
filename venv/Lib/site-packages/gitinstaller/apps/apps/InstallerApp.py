from ..BaseApp import BaseApp, MANY
from tools.install import install

class App(BaseApp):
	"Install a package"
	
	def main(self):
		pkgs = self.args[MANY]
		install(pkgs)
	
	# === PROTECTED ===
	
	def options(self):
		return []
