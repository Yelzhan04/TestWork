from ..BaseApp import BaseApp, MANY
from tools.install import install
from tools.uninstall import uninstall

class App(BaseApp):
	"Uninstall, then reinstall a package"

	def main(self):
		pkgs = self.args[MANY]
		uninstall(pkgs)
		install(pkgs)
	
	def options(self):
		return ["--reinstall"]
