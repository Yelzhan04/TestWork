from ..BaseApp import BaseApp, MANY
from tools.uninstall import uninstall

class App(BaseApp):
	"Uninstall a specific package"
	
	def main(self):
		pkgs = self.args[MANY]
		uninstall(pkgs)
	
	def options(self):
		return ["--uninstall"]
