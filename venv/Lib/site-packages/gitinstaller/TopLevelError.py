class TopLevelError(Exception):
	"These are called to terminate the program in expectable ways."

	def __init__(self, **kwargs):
		self.what = self.__doc__.format(**kwargs)
		Exception.__init__(self, self.what)
