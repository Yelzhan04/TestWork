from os import path
import json

DIR = path.dirname(__file__)
VERSION_F = path.join(DIR, "Version.py")

GIT_URL = "https://github.com/{package}"
CLONE_CMD = "git clone {url} --quiet"

NAME = "git-install"

EXE = NAME + ".py"
BIN = path.join("bin", EXE)

INSTALL_INI = "git-install.ini"
INSTALLED_F = "git-installed.log"

KEY_INCLUDE = "INCLUDE"
KEY_EXCLUDE = "EXCLUDE"
KEY_REQUIRE = "REQUIRE"
