from os import path

class Path(str):

	@staticmethod
	def start(fname):
		return Path(path.normpath(path.dirname(fname)))
	
	def __new__(cls, p):
		return str.__new__(cls, p)
	
	def __add__(self, p):
		if not p:
			return self
		else:
			return Path(path.join(str(self), path.normpath(p)))
